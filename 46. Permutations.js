let ans = [];
let map = new Map();
let permute = function (nums) {
    if (nums.length < 2) return nums;

    return ans;
};
let helper = function (n, nums) {
    if (nums.length < 2) {
        return nums[0];
    }
    
}

//ans.push([nums[i], helper(nums[i], nums.filter(item => item !== nums[i]))]);

console.log(permute([1, 2]));